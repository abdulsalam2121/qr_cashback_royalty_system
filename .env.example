# Database
DATABASE_URL="postgresql://username:password@localhost:5432/qr_loyalty_system"

# JWT Secret
JWT_SECRET="your-jwt-secret-here"

# Stripe
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Firebase Backend (Admin SDK) - OPTIONAL: Use JSON file instead
# Place firebase-service-account.json in backend/config/ directory

# Frontend Firebase Config (Copy to frontend/.env.local with VITE_ prefix)
# Firebase Backend (Admin SDK) - Use backend/config/firebase-service-account.json only

# Other
NODE_ENV="development"
PORT=3001
